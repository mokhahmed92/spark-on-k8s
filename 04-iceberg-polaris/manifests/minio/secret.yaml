# MinIO root credentials
# Values: minioadmin / minioadmin
apiVersion: v1
kind: Secret
metadata:
  name: minio-credentials
  namespace: minio
  labels:
    app: minio
type: Opaque
data:
  # echo -n 'minioadmin' | base64
  MINIO_ROOT_USER: bWluaW9hZG1pbg==
  # echo -n 'minioadmin' | base64
  MINIO_ROOT_PASSWORD: bWluaW9hZG1pbg==
